import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * CoreModule
 * Provides all application-wide singleton services.
 * Import once in AppModule only.
 */
@NgModule({
  imports: [CommonModule],
})
export class CoreModule {}
